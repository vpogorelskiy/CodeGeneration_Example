import SwiftUI

extension CustomNavigation {
    struct TitledView: View {
        let title: String
        let wrappedView: AnyView
        
        var body: some View {
            wrappedView
        }
    }
}
